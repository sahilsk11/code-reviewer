{
  "schema_version": 1,
  "name": "missing-auth-check",
  "crumb_id": "reviewer_correctness_regressions",
  "input": {
    "title": "Allow admins to delete users",
    "body": "Adds an admin-only user deletion endpoint.",
    "repository": "eval/missing-auth-check",
    "pull_request_number": 1
  },
  "repo": {
    "base_files": {
      "app.py": "from dataclasses import dataclass\n\n\n@dataclass\nclass Request:\n    user_id: int\n    is_admin: bool\n\n\nUSERS = {1: \"owner\", 2: \"member\"}\n\n\ndef require_admin(request: Request) -> None:\n    if not request.is_admin:\n        raise PermissionError(\"admin required\")\n\n\ndef delete_user(request: Request, target_user_id: int) -> dict[str, object]:\n    require_admin(request)\n    if target_user_id == request.user_id:\n        raise ValueError(\"cannot delete yourself\")\n    removed = USERS.pop(target_user_id, None)\n    return {\"deleted\": removed is not None}\n",
      "README.md": "# Tiny API\n\nAdmin routes must call `require_admin` before mutating users.\n"
    },
    "changed_files": {
      "app.py": "from dataclasses import dataclass\n\n\n@dataclass\nclass Request:\n    user_id: int\n    is_admin: bool\n\n\nUSERS = {1: \"owner\", 2: \"member\"}\n\n\ndef require_admin(request: Request) -> None:\n    if not request.is_admin:\n        raise PermissionError(\"admin required\")\n\n\ndef delete_user(request: Request, target_user_id: int) -> dict[str, object]:\n    if target_user_id == request.user_id:\n        raise ValueError(\"cannot delete yourself\")\n    removed = USERS.pop(target_user_id, None)\n    return {\"deleted\": removed is not None}\n"
    }
  },
  "expected": {
    "must_include": [
      "require_admin",
      "delete_user",
      "non-admin"
    ],
    "expected_files": [
      "app.py"
    ],
    "should_not_include": [
      "nit",
      "style-only",
      "formatting"
    ]
  }
}
